<%- include('partials/header'); %> <!-- Include the header partial -->

<h1>Home</h1>
<p><%= StartingContent %></p>
 
 <% posts.forEach(function(post) { %>
    <h1><%= post.title %></h1>
    <p class="truncated-paragrap"><%= post.content.substring(0,100) + " ..." %> <a href="/posts/<%=post.title%>">Read More</a></p>
 <% }); %>

 

<%- include('partials/footer'); %>
